# Audit of the round-2 fixes — phase 1

**Verdict:** approve

All three r2 findings are fixed in the product by this diff and each was re-verified by running it. BLOCKER (ci.yml built a dotted tox env name): the sqlite-matrix rows now carry explicit dotless `toxenv` values and the step runs `uv run tox -e ${{ matrix.toxenv }}`; `uv run tox c -e <all 11 names>` resolves every cell with exit 0, `uv run tox l` lists them in the same order, and the exact CI command for one cell (`uv run tox -e py313-dj61-sqlite -- -q --cov=admin_errors --cov-fail-under=90`) ends `py313-dj61-sqlite: OK`, 14 passed, coverage 100% against the 90% floor. The regression test is now executable proof: rerunning the new `test_ci_matrix_lists_exactly_the_profile_sqlite_cells` against the pre-fix ci.yml (extracted from 02c22aa into a temp tree) fails with AssertionError, whereas the old dot-stripping version could not, and the expected names are derived from tox.ini's `env_list` via `_expand_tox_factors` rather than a second hand-written literal, so the two files cannot drift apart. MINOR 1: `.github/workflows/ci.yml` is now in the sdist include list — verified by building an sdist to /tmp (`tar tzf` shows the file) and running `pytest tests/test_toolchain.py` from the unpacked sdist (6 passed); the wheel is unaffected (7 files, no .github). MINOR 2: `[project.urls]` now points at github.com/korkholeh/… — confirmed in the built wheel's METADATA (both Project-URL lines); no stale `okorkh` remains outside the review document. Nothing was rejected, so no rejection needs arguing. Full suite green (`uv run pytest -q` → 14 passed) and lint clean (ruff check, ruff format --check, django check). No contradiction with the spec, ARCHITECTURE.md or a logged decision; all four changes are recorded in DECISIONS.md under `## p01-review_fix2` with rationale and alternatives. Working tree left untouched (builds went to /tmp) and no processes left running.
