# Audit of the round-2 fixes — phase 3

**Verdict:** approve

All four REVIEW-r2 findings are fixed in the product/tests by this diff, each with a test that fails without the fix. Verified the MAJOR independently: the nested-raise traceback yields 4 frames by default and exactly 1 ('_innermost') under MAX_FRAMES=1, so the rewritten test discriminates removal of the truncation and a flipped slice direction (spec §5 line 168). _normalize_level maps DEBUG->info and unknown/oversized values -> error, keeping Issue.level inside the four choices and within max_length=10 (verified live: DEBUG->info, somethinglong->error, None->error, no raise); _querydict_to_dict preserves repeated GET/POST keys with scrubbing unaffected (cleanse_setting recurses into lists) and keeps the plain-dict branch via hasattr(post, "lists"); the three-sample query bound is tightened to 8 so a per-row insert loop (9) trips it; the root-logger side effect is documented in CHANGELOG Unreleased. 113 passed in 0.27s, full lint gate exit 0, no migration change. The new module-level models import in capture.py adds no import risk (capture already imports storage, which imports models; models imports nothing from the package; ready() reaches capture only lazily). No consumer in src/ reads block["post"]/["query"] as str. The only declined item was the review's explicitly optional second-date suggestion, argued in DECISIONS.md.
