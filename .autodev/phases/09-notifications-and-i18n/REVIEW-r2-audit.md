# Audit of the round-2 fixes — phase 9

**Verdict:** approve

All four r2 findings are genuinely fixed in this diff, and the one rejection is properly argued. The blocker (e2e red because the notification case shared the `demo_app.views.keyerror` culprit with `test_admin_ui.py`) is fixed by pointing the case at `/logged/`: that view calls `logger.exception` inside an `except`, so `record.exc_info` is set and `culprit` resolves to `demo_app.views.logged` (unlike `/warning/`, which capture.py:473-478 leaves with `culprit=""` — the fix session's DECISIONS entry documents catching that on a live run); grep confirms no other e2e spec hits `/logged/`, and `demo_seed`'s culprits are all `demo_app.management.commands.demo_seed._*`, so nothing collides. I re-ran the documented command end to end: `make e2e-up && uv run --extra e2e pytest e2e -q && make e2e-down` → 19 passed, 1 deselected, exit 0, port 8000 free afterwards, so PLAN.md T13's e2e claim is now true. Minor 1 is fixed with a never-cleared `data-ae-copy-count` incremented only on a resolved clipboard write, while the 2s timer reverts only the label; grep shows no remaining reference to the removed `data-ae-copied-state` in CSS, JS or tests. Minor 2 is fixed: CHANGELOG and the `notifications.py` docstring now describe the real predicate (backend resolves + reason in `NOTIFY_ON` + optional `is_enabled()`, absent hook = enabled, `EmailNotifier.is_enabled()` being the one that needs recipients), matching notifications.py:46. The nit is fixed: the refreshed `.po` differs only in `#:` references and POT-Creation-Date, and `msgfmt django.po` is byte-identical to the committed `.mo` (which itself is unchanged). The `notified_at`-on-resolve rejection is legitimate under rule 3 — `p09-review_fix2/minor-rejected2` drops the circular "would weaken a test" reason and argues the spec literally (docs/spec.md:498 states the throttle with no resolve carve-out), while tracking the operator-silence documentation as a named Phase 10 README item. Nothing else broke: `uv run pytest -q` is 342 passed / 8 skipped, and ruff check, ruff format --check, django check and `makemigrations --check` are all clean. Only cosmetic leftovers, deliberately not raised as findings: the CHANGELOG reflow left one over-long line, and notifications.py:4 still carries the pre-fix "whether there is anyone to notify" phrase immediately above the corrected paragraph.
