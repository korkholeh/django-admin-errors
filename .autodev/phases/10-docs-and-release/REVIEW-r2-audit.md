# Audit of the round-2 fixes — phase 10

**Verdict:** approve

All five REVIEW-r2 findings are fixed in the product by this diff; none was rejected, so no DECISIONS.md argument was owed. The MAJOR (test_readme_faq_maps_each_silent_failure_to_an_instrument slicing on the first literal \"FAQ\") is fixed by anchoring on `^## .*FAQ.*$` and bounding to the next `^## ` heading; I mutation-proved it in memory (no files modified): deleting the whole FAQ section now fails with \"no 'FAQ' section found in README\", and removing a single required term fails naming that term — both passed before the fix. Minor #1 is fixed the same way: the spec-17 check now matches `^#{2,3} ` heading lines only, and deleting Retention / Permissions / Non-goals / How it works each now fails naming the right fragment; all 12 fragments resolve to real headings. Minor #2 (stale docstring) and #3 (garbled traceback sentence) are corrected, and the new traceback wording is factually right — build_frames preserves Django's outermost-to-innermost order, traceback.html iterates it unreversed, and MAX_FRAMES truncates with frames[-max_frames:], so the raising line really is at the bottom. Minor #4's QA-RESULTS block is genuine evidence, not transcription: session log 098-p10-review_fix2.jsonl shows the real e2e-down/up/pytest/down cycle and its `25 passed, 1 deselected in 25.37s` matches the recorded text exactly; no pid file and nothing listening on 8000 now. Re-ran the gates myself: ruff check clean, ruff format --check 101 files clean, pytest -q 354 passed / 8 skipped. Nothing in the diff contradicts the spec, ARCHITECTURE.md or a logged decision. One minor left: the new docstring cites a pytest nodeid that does not resolve because the referenced test is inside a class.

## [MINOR] Docstring points at an unresolvable pytest nodeid for the email coverage
`e2e/test_manual_qa.py`

The fix for review r2 minor #2 redirects the reader to `tests/test_notifications.py::test_regression_sends_one_email` (e2e/test_manual_qa.py:4-5). That nodeid does not resolve: the test lives at tests/test_notifications.py:195 inside `class TestEmailNotifierEndToEnd` (line 169). `uv run pytest -q "tests/test_notifications.py::test_regression_sends_one_email"` prints `(no match in any of [<Module test_notifications.py>])` / `no tests ran in 0.00s`. The claim that the email path is covered elsewhere is true, but the pointer an operator would copy-paste is broken. The same broken nodeid appears twice in e2e/plans/manual-qa.plan.yaml (lines 21 and 93), written by review_fix1 — worth correcting in the same pass since the diff already touches this claim.

**Fix:** Use the full nodeid `tests/test_notifications.py::TestEmailNotifierEndToEnd::test_regression_sends_one_email` in the e2e/test_manual_qa.py docstring, and update the two occurrences in e2e/plans/manual-qa.plan.yaml to match.
